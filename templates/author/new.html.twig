{% extends 'base.html.twig' %}

{% block title %}Ajouter un auteur{% endblock %}

{% block body %}
<div class="container">
    <h1>Ajouter un auteur</h1>

    {{ form_start(form, {'attr': {'class': 'form-card'}}) }}
        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        <button type="submit" class="btn">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('author_index') }}" class="back-link">Retour à la liste</a>
</div>
{% endblock %}
